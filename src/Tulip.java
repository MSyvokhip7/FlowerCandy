public class Tulip extends Flower {
    public Tulip(int height, int price, int freshness) {
        this.setHeight(height);
        this.setFreshness(freshness);
        this.setPrice(price);
        this.setType("Chocolate");
    }
}
